import type { ArchNode, ArchConnector, NarrativeChapter } from '@/lib/types';

export const nodes: ArchNode[] = [
  {
    id: 'ingestion_pipeline',
    label: 'Ingestion pipeline',
    sublabel: 'From brand materials to encoded knowledge',
    category: 'core',
    status: 'in-development',
    position: { x: 80, y: 60 },
    what: 'The tooling that takes existing brand materials — decks, briefs, guidelines, past work, chemistry session notes — and transforms them into structured nucleus slots. Two-path logic: document parsing for static assets, extraction prompting for unstructured content. The pipeline is reusable. The content run through it is client-specific and billed separately.',
    why: 'Right now a strategist hand-writes the knowledge file. That is the right approach for the prototype — it forces precision and surfaces what is actually known versus assumed. At scale, there needs to be a structured interface for encoding, reviewing, and versioning brand knowledge that does not require writing markdown.',
    decisions: [
      'The pipeline is capex. The content is client cost: the reusable tooling that processes any brand\'s materials into nucleus slots is infrastructure investment. Running a specific client\'s materials through it is account work.',
      'Extraction prompting for unstructured content: not every brand asset is a structured document. The ingestion pipeline uses prompted extraction to pull slot-appropriate content from decks, meeting notes, and qualitative research.',
      'Slot-level validation on ingest: the pipeline checks whether an ingested claim has a documented source before writing it to a slot. Undocumented claims get flagged, not silently encoded.',
    ],
    status_detail: 'The Hamilton Beach build proved the manual encoding approach works. The pipeline tooling to make it systematic is the next infrastructure build.',
  },
  {
    id: 'knowledge_stores',
    label: 'Knowledge Stores',
    sublabel: 'What the system knows about a brand',
    category: 'knowledge',
    status: 'built-partial',
    position: { x: 80, y: 220 },
    what: 'The knowledge stores are the structured, encoded representation of everything the system knows about a brand. Not a document repository. Not a search index. A set of named slots, each with a defined purpose, a confidence level, a last-updated date, and a documented source. The system tells you what it does not know rather than filling gaps with plausible-sounding invention.',
    why: 'The way knowledge is stored determines the quality of what comes out. Free-text documents produce inconsistent retrieval. A structured slot model means every piece of knowledge has a defined home, a defined shape, and a defined relationship to the other slots. When the system returns audience context, it comes from the audience slot — not from a vector search across 80 slides of deck content.',
    decisions: [
      'Slot-level updates, not document-level: when something changes, you overwrite the slot, not the file. The update log captures what changed, when, and why.',
      'Confidence levels on every slot: a slot with a low confidence score and a \'not yet encoded\' flag is better than a missing slot or a wrong one. The system surfaces its own uncertainty.',
      'Sources documented per slot: every encoded claim traces back to a specific source document, conversation, or client session. This matters for trust and for knowing what to update when things change.',
    ],
  },
  {
    id: 'brand_strategy',
    label: 'Brand strategy',
    sublabel: 'Positioning, values, the job',
    category: 'knowledge',
    status: 'in-development',
    parent: 'knowledge_stores',
    position: { x: 80, y: 375 },
    what: 'The stable center of what a brand is. Positioning, values, the single declarative sentence of what the brand exists to do. The human truths that the brand is built around. This slot changes rarely — it is the anchor everything else references.',
    status_detail: 'Being developed in collaboration with Liz Geil, head of strategy at Colle McVoy. The strategic framework for how this slot gets structured across brands is WIP.',
  },
  {
    id: 'core_concept',
    label: 'Core concept',
    sublabel: 'The organizing creative principle',
    category: 'knowledge',
    status: 'in-development',
    parent: 'knowledge_stores',
    position: { x: 80, y: 490 },
    what: 'The big idea. The organizing creative principle that everything executional references. In the Hamilton Beach prototype this includes platform lanes — named campaign territories, each with an insight, an audience, visual direction, and operational tone rules. The core concept slot is what separates a brand intelligence system from a style guide.',
    status_detail: 'WIP. The structure for encoding a core concept generically (not just for one brand) is part of the current architecture work.',
  },
  {
    id: 'audience_profiles',
    label: 'Audience profiles',
    sublabel: 'Human truths, segmented definitions',
    category: 'knowledge',
    status: 'built-partial',
    parent: 'knowledge_stores',
    position: { x: 80, y: 605 },
    what: 'Encoded human truths and segmented audience definitions. Not demographics — the emotional and behavioral reality of the people the brand is trying to reach. Organized by segment, each with a core insight and the implication for how the brand shows up.',
  },
  {
    id: 'proof_constraints',
    label: 'Proof + constraints',
    sublabel: 'Facts, guardrails, compliance',
    category: 'knowledge',
    status: 'built-partial',
    parent: 'knowledge_stores',
    position: { x: 80, y: 720 },
    what: 'Documented facts, brand guardrails, compliance requirements, and regulatory constraints. The things that must be true and the things that must not appear. This is what the QA coherence agent checks against. The operational copy rules in the Hamilton Beach build live here: testable, pass/fail, not vague.',
  },
  {
    id: 'discipline_outputs',
    label: 'Discipline outputs',
    sublabel: 'What each function produces for the brand',
    category: 'knowledge',
    status: 'in-development',
    parent: 'knowledge_stores',
    position: { x: 80, y: 835 },
    what: 'The encoded product of each discipline\'s core thinking as it applies to the brand. Not process maps. Not how departments work day-to-day. The artifact each function produces that everything downstream depends on. Content strategy. Creative territory and intent. Social and earned strategy. Design system intent. Communications architecture. Media strategy (low confidence — flagged).',
    why: 'This is the slot type that turns the Nucleus from a smart brief tool into a platform. When a media planning agent, a social content satellite, and a design pipeline tool all draw from the same encoded discipline outputs, the brand holds together across channels without requiring a human to translate between them each time.',
    decisions: [
      'Encode the output, not the process: the nucleus holds the artifact, not the methodology. The process is how it gets produced and encoded. The slot holds the result.',
      'Media strategy is flagged low confidence: this is intentional, not an omission. The domain knowledge required to encode media strategy correctly at this level of structure requires a domain partner. A low-confidence slot is better than a confidently wrong one.',
      'Social and earned strategy is in the first build tier: initially placed in a \'harder to encode\' bucket because of domain distance, but actually close enough to strategy and creative to be encodable with the current team.',
    ],
  },
  {
    id: 'design_token_store',
    label: 'Design token store',
    sublabel: 'Format-neutral, tool-agnostic',
    category: 'knowledge',
    status: 'in-development',
    parent: 'knowledge_stores',
    position: { x: 80, y: 950 },
    what: 'The structured, format-neutral representation of a brand\'s visual system. Color, typography, spacing, component logic stored as JSON. Not a Figma file. Not an InDesign library. A source that gets translated to whatever tool needs it downstream. Figma gets one translation. Adobe gets another. Both draw from the same source.',
    why: 'DCP has built powerful tool-specific implementations — juiced-up InDesign files, Adobe panels with AI ingestion, custom style systems. Each one is a one-off. The design token store is the architecture that makes those one-offs systematic: build the source once, translate to tools on demand.',
    decisions: [
      'Two separable things in this slot: design intent (the documented purpose and philosophy of the visual system — why decisions were made) and design tokens (the actual values). Intent is readable by a creative director, a copywriter, or an AI generating a brief. Tokens feed production pipelines.',
      'Format-neutral JSON as the canonical format: not Figma variables, not Adobe styles. A format that can be read by anything and translated to any tool.',
      'Figma for digital workflows, Adobe for production workflows: Figma is not universally adopted across the agency. It is used for specific digital workflows where it is well-founded. Adobe is the core production tool. The design token store serves both without privileging either.',
    ],
    open_questions: [
      'Who owns the common denominator schema for communicating brand design principles to both Figma and Adobe? This is a meaty problem. Doug Everhart, Patrick Anders, and Matthew Ulstead are the right people to work this with.',
      'How do design tokens get versioned when a brand refreshes? Do satellites automatically get the updated tokens, or is there a deliberate promotion step?',
    ],
  },
  {
    id: 'account_knowledge',
    label: 'Account knowledge',
    sublabel: 'What the brand has done and learned',
    category: 'evolving',
    status: 'specced',
    parent: 'knowledge_stores',
    position: { x: 80, y: 1065 },
    what: 'The knowledge the brand accumulates over the life of an account. Not what the brand is — that is the stable center. What the brand has done: past campaigns, retrospective feedback on current work, strategic planning decisions, historical work product. This slot is not static. It evolves through a structured input protocol and feeds back into the strategy and creative intent slots over time.',
    why: 'An intelligence system that only knows what a brand is at the moment of encoding will drift from reality as the brand evolves. Account knowledge is the mechanism that keeps the system current without requiring full re-encoding cycles. A quarterly retrospective input that gets processed and appended to the right slots is the difference between a system that stays alive and one that goes stale.',
    how: 'A structured input interface allows an account lead to log a retrospective note or strategic planning decision. The processing step is an agent: it reads the input, identifies which slots it affects, and proposes appends with attribution. A human reviews before it commits. The nucleus uses an append-and-version model — not slot overwrite. What was true before is preserved. What is new is added with a timestamp and source.',
    decisions: [
      'Append model, not overwrite: institutional memory requires that you can see what changed and when, not just what is true now.',
      'Human review before commit: the agent proposes, the human approves. This is the right division of labor for something that will affect all downstream outputs.',
      'Account knowledge mostly informs strategy and brand logic but can shift creative intent: a retrospective that reveals a tone direction is not working is a creative intent update, not just a strategic note.',
    ],
    open_questions: [
      'What is the right cadence for account knowledge inputs? Too frequent and the system is noisy. Too infrequent and it misses meaningful shifts.',
      'How does the system handle conflicting signals — when new account knowledge contradicts an encoded anchor?',
    ],
  },
  {
    id: 'media_strategy',
    label: 'Media strategy',
    sublabel: 'Low confidence — flagged, not omitted',
    category: 'low-confidence',
    status: 'specced',
    parent: 'knowledge_stores',
    position: { x: 80, y: 1180 },
    what: 'A discipline output slot flagged as low confidence. The domain knowledge required to encode media strategy correctly at this level of structure requires a domain partner that has not yet been engaged. The slot exists in the architecture deliberately — as a flagged gap, not a silent omission. When the domain partner is identified, this slot gets filled.',
  },
  {
    id: 'nucleus',
    label: 'The Nucleus',
    sublabel: 'Brand intelligence API',
    category: 'core',
    status: 'built',
    position: { x: 520, y: 220 },
    what: 'The Nucleus is a brand intelligence API. It sits between the knowledge stores and everything downstream. It does not write copy, design ads, or generate content. It assembles and returns structured creative context — the minimum useful package a downstream agent or tool needs to do its job with brand coherence.',
    why: 'Every agency\'s institutional knowledge lives in senior people\'s heads. It degrades at every handoff, resets when teams change, gets reinterpreted when a brief leaves something ambiguous. The Nucleus is the architectural answer to that problem: a single source of brand truth that travels with the work, not the people.',
    how: 'A single Next.js API endpoint receives a plain-language request plus metadata from a satellite. It injects the knowledge file as a cached system prompt, runs a single Claude call that classifies intent and assembles a context package simultaneously, and returns structured JSON. Prompt caching means the knowledge file is not reprocessed on every call. The model extracts and assembles from encoded knowledge — it does not invent.',
    decisions: [
      'Single-call architecture: the system originally made two sequential calls (classify, then assemble). Merged into one to cut latency and reduce the opportunity for drift between steps.',
      'Prompt caching on the knowledge file: at scale, re-processing 580+ lines of brand knowledge on every call is expensive and slow. Anthropic cache_control solves this.',
      'The model does not freestyle: every field in the response traces back to a documented source. If the knowledge file does not contain something, the system says so rather than generating a plausible-sounding answer.',
      'Brand-agnostic API: nothing in the API code is client-specific. The knowledge file and satellite specs are the client-specific parts. The API shape, caching layer, and streaming all transfer to any brand.',
    ],
    open_questions: [
      'At what point does a single knowledge file per brand become insufficient? Does a large brand need a knowledge file per campaign layer?',
      'How does the Nucleus handle conflicting signals — when a retrospective feedback entry contradicts an encoded brand anchor?',
    ],
    status_detail: 'Working prototype built for the Hamilton Beach pitch. Single-brand, single-tenant. Generalization to multi-brand is the next build milestone.',
  },
  {
    id: 'interpretation_layer',
    label: 'Interpretation Layer',
    sublabel: 'Intent parsing · depth calibration · format translation',
    category: 'core',
    status: 'built-partial',
    position: { x: 520, y: 420 },
    what: 'The interpretation layer is the reasoning step that happens inside the Nucleus between receiving a request and returning a context package. It is not a separate service — it is the intelligence of the system. It does three things: parses the satellite\'s declared intent to determine which knowledge domains are relevant; calibrates how much of each domain to return; and translates the context into the format the requesting agent can use.',
    why: 'A system that returns everything it knows on every request produces drift, not coherence. A copywriting agent asking for Reddit content does not need design tokens or media planning data. The interpretation layer is what makes the difference between a useful context package and a data dump. More is not better when working with AI — unnecessary context introduces noise and pulls generation away from what matters.',
    how: 'Each satellite maintains a SKILL.md file that declares its identity, what it does, what context it needs, and what format it expects back. This is the same pattern used in Claude\'s own skills system. The Nucleus reads the SKILL.md at request time and uses it to calibrate the response. The AI reasoning happens at the handoff — not rigid schema matching, but genuine interpretation of declared intent against available knowledge.',
    decisions: [
      'Pull model with declared intent: satellites pull what they need by describing what they are doing. The Nucleus decides relevance. Nothing gets pushed that was not asked for.',
      'SKILL.md pattern for satellite identity: each agent self-maintains its own skills file. This makes the agent-nucleus interface self-documenting and extensible without requiring changes to the Nucleus codebase.',
      'Minimum useful context principle: the goal is the smallest context package that allows the satellite to do its job with full brand coherence. Not comprehensive. Not a data dump.',
      'AI-to-AI reasoning at the handoff: the interpretation step is not rules-based. It uses the same model that powers the Nucleus to read the satellite\'s SKILL.md and decide what to surface. This means it gets better as the model improves.',
    ],
    open_questions: [
      'Should the Nucleus log what each satellite type actually uses from the context package it receives? Over time, those logs would reveal patterns that could be encoded as registered satellite profiles — making the interpretation more precise without requiring the satellite to re-declare intent on every call.',
      'What happens when a satellite\'s SKILL.md is vague or incomplete? Does the Nucleus ask for clarification, make a conservative assumption, or surface a warning?',
    ],
    status_detail: 'The Hamilton Beach prototype has a working version of this for creative satellites. The SKILL.md formalization and the multi-consumer depth calibration are the next design steps.',
  },
  {
    id: 'knowledge_ui',
    label: 'Knowledge management UI',
    sublabel: 'Encode, review, version — without markdown',
    category: 'core',
    status: 'specced',
    position: { x: 520, y: 660 },
    what: 'A structured interface for encoding, reviewing, and versioning brand knowledge at the slot level. Replaces the markdown file at scale. Allows strategists to encode and update brand knowledge without editing raw files. Allows clients — eventually — to see, validate, and contribute to the knowledge the system holds about their brand.',
    decisions: [
      'Slot-level interface, not document editor: the UI is organized around the nucleus schema — you edit a slot, not a file. This enforces the structure and makes the confidence/source metadata visible.',
      'Client-facing visibility is a later milestone: the knowledge file is currently agency-side. Making it client-visible requires thinking through what clients see, what they can edit, and what stays internal.',
    ],
  },
  {
    id: 'multi_brand',
    label: 'Multi-brand infrastructure',
    sublabel: 'Per-brand knowledge isolation',
    category: 'core',
    status: 'specced',
    position: { x: 520, y: 840 },
    what: 'Tenant model — per-brand knowledge store isolation. Same API, different knowledge file per brand or client. The API is brand-agnostic. The knowledge files are client-specific. Multi-brand infrastructure is the generalization work that makes Brand Gravity a platform rather than a prototype.',
  },
  {
    id: 'satellites',
    label: 'Satellites',
    sublabel: 'Agents that call the nucleus',
    category: 'satellite',
    status: 'built-partial',
    position: { x: 960, y: 220 },
    what: 'Satellites are separate agents that call the Nucleus API and use the context package to produce actual creative output. They carry zero brand knowledge of their own. Everything comes from the nucleus. This is the architectural principle: brand intelligence is centralized, creative execution is distributed.',
    why: 'If brand knowledge lives inside each satellite, you have as many versions of the brand as you have satellites. When something changes, you change it everywhere. When a new satellite is added, it needs to be trained on the brand. The satellite pattern solves this: one source of truth, infinite consumers.',
    decisions: [
      'Satellites carry no brand knowledge: this is non-negotiable. It is what makes the architecture defensible over time.',
      'Each satellite maintains a SKILL.md: declares identity, purpose, context needs, expected response format. The nucleus reads it. The agent-nucleus interface is self-documenting.',
      'Agents are experimental, replaceable, and multiplied. The nucleus is stable infrastructure. Collapsing the two breaks the system when you try to change anything.',
    ],
  },
  {
    id: 'creative_pov',
    label: 'Creative POV satellite',
    sublabel: 'Brief generation from nucleus context',
    category: 'satellite',
    status: 'built',
    parent: 'satellites',
    position: { x: 1110, y: 375 },
    what: 'Calls the nucleus with a job-to-be-done and receives brand context calibrated for brief generation. Produces a creative POV document: the strategic and creative framing a creative team needs to start work.',
  },
  {
    id: 'reddit_satellite',
    label: 'Reddit content satellite',
    sublabel: 'Native content in 4 sub-modes',
    category: 'satellite',
    status: 'built',
    parent: 'satellites',
    position: { x: 1110, y: 490 },
    what: 'Generates Reddit-native content in four sub-modes: organic reply, organic new post, promoted text, conversation ad. Each sub-mode has its own structural rules encoded in the satellite\'s spec. The nucleus provides brand context. The satellite applies platform-native judgment about what belongs on Reddit.',
    decisions: [
      'Four sub-modes because Reddit has four distinct content contexts with different native aesthetics and different brand risk profiles. A single \'Reddit post\' generator would average across them and produce content that fits none well.',
    ],
  },
  {
    id: 'copywriting_agent',
    label: 'Copywriting agent',
    sublabel: 'Batch ad copy for production ingestion',
    category: 'satellite',
    status: 'specced',
    parent: 'satellites',
    position: { x: 1110, y: 605 },
    what: 'Calls the nucleus once, uses the context package as a system prompt, and generates 30–50 rows of ad copy as a CSV formatted for direct ingestion into CreateTotally automation pipelines. The nucleus call happens once per batch — not per row. The context is stable; the variations are generated against it.',
  },
  {
    id: 'qa_agent',
    label: 'QA coherence agent',
    sublabel: 'Automated pass/fail against copy rules',
    category: 'satellite',
    status: 'specced',
    parent: 'satellites',
    position: { x: 1110, y: 720 },
    what: 'Checks generated content against the operational copy rules encoded in the nucleus. Returns pass/fail per rule with a reasoning trace. Not a vibe check — a structured test against documented brand standards. The three copy rules per lane in the Hamilton Beach build are the prototype of this: \'Is this specific to a food and an action?\' has a clear answer. \'Be warm\' does not.',
  },
  {
    id: 'briefing_agent',
    label: 'Briefing agent',
    sublabel: 'Calls the nucleus — is not part of it',
    category: 'agent',
    status: 'specced',
    badge: 'AGENT',
    position: { x: 1110, y: 835 },
    what: 'An agent that takes a job to be done as input, calls the nucleus to retrieve brand context across all relevant discipline slots, and produces a holistic brief — then breaks it into department-specific briefs for strategy, creative, design, media, social, and account. A single brief intake produces a full suite of departmental starting points.',
    why: 'Brief quality failure is one of the four structural problems Brand Gravity is built to address. Most brief failures happen not because nobody knows what to write, but because the person writing the brief has incomplete access to the encoded brand knowledge that should inform it. The briefing agent makes that knowledge available at the moment of brief creation, not after.',
    decisions: [
      'The briefing agent is an agent, not a nucleus component: this distinction is architecturally critical. The nucleus is stable. Agents are experimental. The briefing agent calls the nucleus — it does not live inside it.',
      'Holistic brief first, then department-specific breaks: the agent produces a unified brief for the job to be done, then derives department-specific versions from it. Departments get what they need without duplicating the intake work.',
      'Required inputs across all departments: part of the briefing agent design is defining the minimum required inputs for each department\'s brief. This is both a product design problem and a knowledge architecture problem — what does each discipline need to start work?',
    ],
  },
  {
    id: 'the_machine',
    label: 'The Machine',
    sublabel: 'Stagwell / Code + Theory — external, deferred',
    category: 'external',
    status: 'deferred',
    style: 'dashed',
    position: { x: 510, y: -130 },
    what: 'Stagwell\'s orchestration platform, being built by Code + Theory. Built with SDK and MCP philosophies — meaning it exposes interfaces that other agents and tools can connect to via the Model Context Protocol standard.',
    why_deferred: 'DCP cannot tie their roadmap to C+T\'s timeline. Brand Gravity is designed to operate fully standalone. The Machine becomes an optional distribution channel, not a dependency. The adapter gets written when C+T publishes a spec.',
    strategic_context: 'The risk with The Machine is schema ownership. If C+T defines how brand knowledge gets structured inside their platform, they own the container and DCP fills it. The protection is architectural: if the nucleus sits outside The Machine and gets called via MCP, DCP has used their infrastructure without giving up the intelligence layer. The Machine becomes a runtime. The nucleus remains the brain. The goal at Adobe Summit: position DCP as the reference implementation for what a brand knowledge layer looks like inside The Machine\'s environment — not a consumer of their standard, but a co-definer of it.',
    decisions: [
      'Self-describing output schema: the nucleus returns structured JSON where every field is labeled, typed, and sourced. This is interpretable by almost anything downstream, including whatever schema C+T eventually settles on. The adapter is trivial to write when the spec exists.',
      'Agility advantage: DCP is smaller and less complex than C+T\'s build. They will ship faster. The prototype proves it. Use that lead to establish the standard rather than wait to conform to one.',
    ],
    open_questions: [
      'What does The Machine\'s orchestration layer actually expose? What tools, what data, what runtime?',
      'Is the goal to run Brand Gravity satellites inside The Machine\'s environment, or to have The Machine call the Brand Gravity nucleus as a service?',
    ],
  },
  {
    id: 'adobe_pipeline',
    label: 'Adobe pipeline',
    sublabel: 'InDesign, Firefly, production automation',
    category: 'external',
    status: 'existing-partial',
    position: { x: 40, y: 1340 },
    what: 'DCP has built high-functioning InDesign files with full style coverage and custom panels that use AI to ingest brand information and generate production-ready layouts. These work. They are one-offs. The design token store is what connects them to the nucleus — brand tokens flow from the knowledge store into the Adobe environment systematically rather than being manually re-entered per project.',
  },
  {
    id: 'figma_pipeline',
    label: 'Figma pipeline',
    sublabel: 'Digital workflows, scoped adoption',
    category: 'external',
    status: 'existing-partial',
    position: { x: 320, y: 1340 },
    what: 'Figma is used for specific digital workflows at DCP. It is not universally adopted — it requires upskilling and is expensive at scale. The architecture does not require universal Figma adoption. The design token store translates to Figma format for the workflows where Figma is the right tool. Everything else gets the Adobe translation.',
  },
];

export const connectors: ArchConnector[] = [
  {
    id: 'brand_materials_to_ingestion',
    from: 'brand_materials_input',
    to: 'ingestion_pipeline',
    label: 'Decks, briefs, guidelines, session notes',
    description: 'Raw brand materials enter the system through the ingestion pipeline. The pipeline parses, extracts, and structures them into nucleus slots. The content is client-specific and billed as account work. The pipeline tooling is reusable infrastructure.',
  },
  {
    id: 'ingestion_to_stores',
    from: 'ingestion_pipeline',
    to: 'knowledge_stores',
    label: 'Structured slot content',
    description: 'The ingestion pipeline writes to named slots with confidence levels and source attribution. It does not append to a flat document.',
  },
  {
    id: 'stores_to_nucleus',
    from: 'knowledge_stores',
    to: 'nucleus',
    label: 'Full encoded knowledge',
    description: 'The interpretation layer has access to all knowledge slots. It selects what is relevant based on the satellite\'s declared intent. Not everything gets returned — only what the satellite needs to do its job.',
  },
  {
    id: 'skill_to_interpretation',
    from: 'satellites',
    to: 'interpretation_layer',
    label: 'SKILL.md — declared intent',
    description: 'The satellite\'s SKILL.md arrives with every request. It declares what the satellite is, what it needs, and what format it expects. The interpretation layer reads it and uses it to calibrate what gets returned. This is AI-to-AI reasoning, not schema matching.',
    style: 'highlighted',
  },
  {
    id: 'interpretation_to_satellite',
    from: 'interpretation_layer',
    to: 'satellites',
    label: 'Context package',
    description: 'The minimum useful context for the requesting satellite: intent classification, audience context, tone direction, copy rules, art direction, proof points, format spec, do-not list, reasoning trace. Shape and depth vary by satellite type.',
  },
  {
    id: 'account_to_stores',
    from: 'account_knowledge',
    to: 'knowledge_stores',
    label: 'Cadenced append',
    description: 'Account knowledge does not overwrite slots — it appends to them with versioning. A retrospective that surfaces a creative direction problem becomes an update to the creative intent slot. Strategic planning decisions update the brand strategy slot. The feedback loop is what keeps the system alive over time.',
    style: 'curved',
  },
  {
    id: 'tokens_to_adobe',
    from: 'design_token_store',
    to: 'adobe_pipeline',
    label: 'Adobe token translation',
    style: 'dashed',
    description: 'Design tokens are translated from the canonical JSON format into Adobe-native styles and structures. The translation layer handles format differences; the source remains canonical and tool-agnostic.',
  },
  {
    id: 'tokens_to_figma',
    from: 'design_token_store',
    to: 'figma_pipeline',
    label: 'Figma token translation',
    style: 'dashed',
    description: 'Design tokens are translated from the canonical JSON format into Figma variables and styles. The translation layer handles format differences; the source remains canonical and tool-agnostic.',
  },
  {
    id: 'nucleus_to_machine',
    from: 'nucleus',
    to: 'the_machine',
    label: 'MCP adapter (when spec exists)',
    style: 'dashed',
    description: 'Brand Gravity stands alone. When C+T publishes a Machine spec, DCP writes an adapter. The adapter is not a dependency. The nucleus remains the source of intelligence regardless of which orchestration layer calls it.',
  },
];

export const narrative: NarrativeChapter[] = [
  {
    title: 'The Problem',
    body: `Marketing organizations are spending more and getting less. A third of all marketing budgets are estimated wasted due to misdirected work. Half of digital ad spend goes behind creative that fails basic brand coherence tests before a dollar of media runs. None of this is a talent problem. It is structural. There is no layer in the current model whose job is to keep what a brand means alive from thinking to execution to scale.

Knowledge lives in senior people's heads. It degrades at every handoff, resets every time a team changes, gets reinterpreted every time a partner fills in what the brief left ambiguous. AI has made this structurally worse: the same tools that generate more content faster also amplify drift faster, because they have no stable source of brand truth to draw from.`,
  },
  {
    title: 'The Approach',
    body: `Brand Gravity is built to solve this. Not by centralizing creative decision-making. Not by replacing human judgment. By encoding what is already known — the brand's stable center, its creative intent, its audience truths, its design logic — into a system that makes that knowledge available at the moment work is being done.

The organizing principle: intelligence-first is an encoding discipline, not a generation discipline. Encoding creates stable context within which generation and human judgment operate. The system does not write ads. It ensures that when ads get written, they are written with the full benefit of everything the brand has ever learned about itself.`,
  },
  {
    title: 'The Architecture',
    body: `The system has five interacting zones. They do not run in sequence. They interact.

The Knowledge Stores are the structured, encoded representation of everything the system knows about a brand — organized into named slots, each with a confidence level and a documented source. The Ingestion Pipeline transforms raw brand materials into those slots. The Nucleus is the brand intelligence API that sits between the stores and everything downstream. The Interpretation Layer is the reasoning step inside the Nucleus that determines what to return and in what form. Satellites are the agents that call the Nucleus and produce actual output — they carry zero brand knowledge of their own.

Each zone has a defined job. The connections between them are as important as the zones themselves.`,
  },
  {
    title: 'What Is Built',
    body: `One working prototype — the Hamilton Beach build — proved the core architectural thesis: a single source of brand truth can serve multiple output types, the knowledge degrades gracefully rather than silently, and the API is brand-agnostic.

The same architecture that produced Reddit posts also produced creative briefs, video briefs, and ad copy batches. The brand held together across all of them. The operational copy rules — the things the QA agent checks — worked as testable pass/fail criteria rather than vague direction. The confidence level system surfaced gaps rather than filling them silently.

The prototype proved the concept. What is built next is the infrastructure that makes it repeatable.`,
  },
  {
    title: 'What Gets Built Next',
    body: `The generalization work has five components:

The ingestion pipeline makes brand encoding systematic — a strategist should not need to hand-write markdown to populate a knowledge file. The multi-brand infrastructure generalizes the single-tenant prototype into a platform that serves multiple clients from the same API. The knowledge management UI replaces the markdown file with a structured interface for encoding, reviewing, and versioning brand knowledge. The interpretation layer formalization turns the current working prototype into a designed system with explicit SKILL.md contracts between satellites and the Nucleus. The design token store connects DCP's existing Adobe and Figma implementations to a canonical, tool-agnostic source.

Each one is a reusable infrastructure asset. The client engagement populates it. That distinction is the capex argument.`,
  },
  {
    title: 'Open Questions',
    body: `The architecture has live uncertainty built into it deliberately. These are not gaps — they are the questions the next phase of work is designed to answer.

At what point does a single knowledge file per brand become insufficient? How does the Nucleus handle conflicting signals between encoded anchors and new retrospective data? What is the right cadence for account knowledge inputs — frequent enough to stay current, infrequent enough to avoid noise? Who owns the common denominator schema for communicating brand design principles across both Figma and Adobe? What does The Machine's orchestration layer actually expose, and is the goal to run Brand Gravity satellites inside it, or to have it call the nucleus as a service?

These questions are surfaced because they are real. A system that hides its uncertainty produces confident wrong answers. A system that flags its uncertainty produces calibrated useful ones.`,
  },
];
